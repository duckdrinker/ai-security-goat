# Triggers mcp-server-runs-as-privileged: MCP server Deployment with no
# securityContext.runAsNonRoot or runAsUser anywhere in the pod/container spec.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-tools-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-tools-server
  template:
    metadata:
      labels:
        app: mcp-tools-server
    spec:
      containers:
        - name: mcp-tools-server
          image: internal-registry/mcp-tools-server:1.4.0
          command: ["python", "-m", "mcp_server.main"]
