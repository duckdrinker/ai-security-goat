# Triggers mcp-server-runs-as-privileged: MCP server systemd unit with no
# User= directive -- runs as root, systemd's default.
[Unit]
Description=MCP Tools Server
After=network.target

[Service]
ExecStart=/usr/bin/python3 -m mcp_server.main
Restart=on-failure

[Install]
WantedBy=multi-user.target
